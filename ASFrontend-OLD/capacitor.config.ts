import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.awesomeshop.frontend',
  appName: 'awesomeshop',
  webDir: 'dist/asfrontend',
  bundledWebRuntime: false,
};

export default config;
